$(document).ready((function () { $("[data-bs-tooltip]").tooltip() })), $(".datepicker").each((function () { new Pikaday({ field: this }) }));
if (window.history.replaceState) { //Para que no hayan duplicidad en los formularios
    window.history.replaceState(null, null, window.location.href);
}